language: python

python: 
    - "3.7.0"

install:
    - pip install -r requirements.txt

script:
    - pytest test_api_endpoints.py --cov=api --cov-report term-missing
    
after_success:
    - coveralls
